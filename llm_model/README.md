# Local LLM Models

Place or symlink local model directories here:

- `./llm_model/gpt-oss-20b`
- `./llm_model/qwen3-4b-4bit-mlx`

The LLM server should expose an OpenAI-compatible API at `http://localhost:8001/v1`.
